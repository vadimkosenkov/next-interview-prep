import { QuestionBlock } from "@/types/question";

export const gitBlock: QuestionBlock ={
  meta:{icon:'🌿',color:'#f05032',ru:{title:'Git & Dev Tools',desc:'Git, CI/CD, Docker, Web Performance Tools'},en:{title:'Git & Dev Tools',desc:'Git, CI/CD, Docker, Web Performance Tools'}},
  topics:{
    git:{icon:'🌿',ru:{title:'Git'},en:{title:'Git'},questions:[
        {id:'g1',ru:{q:'Разница между git merge и git rebase?',options:['merge — сохраняет историю с коммитом слияния; rebase — переписывает историю, линейно переносит коммиты','Нет разницы','rebase быстрее'],correct:0,explanation:'merge: безопасно, сохраняет историю. rebase: чистая линейная история, но опасно на публичных ветках.',theory:'<h4>Merge vs Rebase</h4><pre><code># Merge — сохраняет историю\ngit checkout main\ngit merge feature/auth\n# Создаёт merge commit\n\n# Rebase — линейная история\ngit checkout feature/auth\ngit rebase main\n# Переносит коммиты фичи поверх main\n\n# Золотое правило:\n# Никогда не делай rebase публичных веток!</code></pre>'},en:{q:'Difference between git merge and git rebase?',options:['merge: preserves history with merge commit; rebase: rewrites history, moves commits linearly','No difference','rebase is faster'],correct:0,explanation:'merge: safe, preserves history. rebase: clean linear history, but dangerous on public branches.',theory:'<h4>Merge vs Rebase</h4><pre><code>git merge feature  # merge commit\ngit rebase main    # reapply on top\n# Never rebase public branches!</code></pre>'}},
        {id:'g2',ru:{q:'Что такое git stash?',options:['Временно сохраняет незакоммиченные изменения в стек, очищая рабочую директорию','Удаляет ветку','Откатывает коммит'],correct:0,explanation:'git stash: убираем изменения → переключаемся → возвращаем. stash pop или stash apply.',theory:'<h4>Git Stash</h4><pre><code>git stash           # сохранить\ngit stash -m "wip"  # с сообщением\ngit stash list      # список\ngit stash pop       # применить и удалить\ngit stash apply 0   # применить без удаления\ngit stash drop      # удалить\ngit stash clear     # очистить всё</code></pre>'},en:{q:'What is git stash?',options:['Temporarily saves uncommitted changes to a stack, clearing working directory','Deletes branch','Reverts commit'],correct:0,explanation:'git stash: save changes → switch branch → restore. stash pop or stash apply.',theory:'<h4>Git Stash</h4><pre><code>git stash\ngit stash -m "wip"\ngit stash pop\ngit stash apply 0</code></pre>'}},
        {id:'g3',ru:{q:'Чем git reset отличается от git revert?',options:['reset — удаляет коммиты из истории (опасно для shared); revert — создаёт новый коммит отмены (безопасно)','Нет разницы','revert быстрее'],correct:0,explanation:'git revert HEAD — безопасно для публичных репозиториев. reset --hard удаляет историю.',theory:'<h4>reset vs revert</h4><pre><code># reset — изменяет историю (ОПАСНО на публичных ветках)\ngit reset --soft HEAD~1  # коммит отменён, изменения в staging\ngit reset --hard HEAD~1  # коммит + изменения удалены!\n\n# revert — безопасно, создаёт новый коммит\ngit revert HEAD\ngit revert abc123</code></pre>'},en:{q:'How does git reset differ from git revert?',options:['reset: removes commits from history (dangerous for shared); revert: creates new undo commit (safe)','No difference','revert is faster'],correct:0,explanation:'git revert HEAD — safe for public repos. reset --hard deletes history.',theory:'<h4>reset vs revert</h4><pre><code>git reset --soft HEAD~1  # undo commit, keep changes\ngit reset --hard HEAD~1  # undo commit + changes!\ngit revert HEAD          # safe: new undo commit</code></pre>'}},
      ]},
    cicd:{icon:'🔄',ru:{title:'CI/CD & Docker'},en:{title:'CI/CD & Docker'},questions:[
        {id:'ci1',ru:{q:'Что такое CI/CD?',options:['Continuous Integration (автосборка/тесты при push) и Continuous Delivery/Deployment (автодеплой)','CSS фреймворк','Менеджер зависимостей'],correct:0,explanation:'CI: каждый push → build → tests → lint. CD: автоматический деплой при успехе.',theory:'<h4>CI/CD Pipeline</h4><pre><code># GitHub Actions example\nname: CI\non: [push, pull_request]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n      - run: npm ci\n      - run: npm test\n      - run: npm run build\n  deploy:\n    needs: test\n    runs-on: ubuntu-latest\n    steps:\n      - run: ./deploy.sh</code></pre>'},en:{q:'What is CI/CD?',options:['Continuous Integration (auto build/test on push) and Continuous Delivery/Deployment (auto deploy)','CSS framework','Dependency manager'],correct:0,explanation:'CI: every push → build → tests → lint. CD: automatic deploy on success.',theory:'<h4>CI/CD</h4><pre><code># GitHub Actions\non: [push]\njobs:\n  test:\n    steps:\n      - run: npm test\n      - run: npm run build\n  deploy:\n    needs: test\n    steps:\n      - run: ./deploy.sh</code></pre>'}},
        {id:'ci2',ru:{q:'Что такое Docker и зачем он нужен фронтенд-разработчику?',options:['Контейнеризация: изолированная среда для приложения. Одинаково работает везде — dev, staging, prod','Менеджер версий Node.js','Система мониторинга'],correct:0,explanation:'Docker: "работает на моей машине" → "работает везде". Контейнер = код + зависимости + конфигурация.',theory:'<h4>Docker для фронтенда</h4><pre><code># Dockerfile\nFROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --only=production\nCOPY . .\nRUN npm run build\n\nFROM nginx:alpine\nCOPY --from=0 /app/dist /usr/share/nginx/html\nEXPOSE 80</code></pre><pre><code>docker build -t my-app .\ndocker run -p 3000:80 my-app</code></pre>'},en:{q:'What is Docker and why does a frontend developer need it?',options:['Containerization: isolated environment for app. Works the same everywhere — dev, staging, prod','Node.js version manager','Monitoring system'],correct:0,explanation:'Docker: "works on my machine" → "works everywhere". Container = code + dependencies + config.',theory:'<h4>Docker for frontend</h4><pre><code>FROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build</code></pre>'}},
      ]}
  }
}